{
  "name": "nexodo",
  "scripts": {
    "dev": "bun run build:client && concurrently \"bun run watch:client\" \"bun run watch:server\"",
    "build:client": "bun build src/client/main.ts --outdir public/js --target browser --format esm",
    "watch:client": "bun build src/client/main.ts --outdir public/js --target browser --format esm --watch",
    "watch:server": "bun run --hot src/server/index.ts",
    "build": "bun run build:client",
    "start": "bun run src/server/index.ts",
    "test": "bun test src/client/*.test.ts"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "@types/pg": "^8.15.6",
    "dotenv": "^17.2.3",
    "hono": "^4.10.4",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1"
  }
}